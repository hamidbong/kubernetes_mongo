apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mongo-ingress
  namespace: default  # (ou le namespace de ton service)
spec:
  rules:
    - host: my-mongo.com
      http:
        paths:
        - backend:
          serviceName: mongo-express-service
          servicePort: 8081
